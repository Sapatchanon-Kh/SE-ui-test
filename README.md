# 🤖 Automated Testing: Project Proposal Creation

![Robot Framework](https://img.shields.io/badge/Robot%20Framework-Test_Automation-black?style=for-the-badge&logo=robot-framework)
![Selenium](https://img.shields.io/badge/Library-Selenium-green?style=for-the-badge&logo=selenium)


> **"การทดสอบสร้างแบบเสนอโครงงานใหม่ (Create New Project Proposal)"**

---

## ⚠️ สิ่งที่ต้องเตรียมก่อนรัน (Pre-requisites)

### 🛑 อ่านตรงนี้ก่อนเพื่อน! สำคัญมาก
Script นี้ถูกเขียนมาเพื่อเทสหน้า "สร้างโครงงาน" โดยเฉพาะ ดังนั้นมันจะ **ข้าม (Skip)** ขั้นตอนการสร้างกลุ่มไปเลย

**ก่อนกดรัน เพื่อนต้องมั่นใจว่า Data ในเครื่องตัวเองพร้อมตามนี้:**

| Data | เงื่อนไขที่ต้องมี |
| :--- | :--- |
| **1. User Account** | ต้องมี User ในระบบแล้ว (สำหรับใช้ Login) |
| **2. Project Group** | User คนนั้น **ต้องมีกลุ่มโครงงานอยู่แล้ว** (Status: มีกลุ่มแล้ว) |

> **💡 วิธีเช็คความพร้อม:**
> ให้ลอง Login เข้าเว็บด้วยมือ (Manual) แล้วกดสร้างกลุ่มให้เรียบร้อยก่อน 1 ครั้ง หรือจะ Insert ข้อมูลลง Database โดยตรงก็ได้
> *เป้าหมายคือ: เมื่อบอท Login เข้าไป ต้องพร้อมกดปุ่ม "สร้างแบบเสนอโครงงาน" ได้ทันที*

---

## ⚙️ การตั้งค่า (Configuration)

* **URL:** `http://localhost:5173/` (หรือพอร์ตที่รันอยู่)
* **Login User:** `B6712123` (ตัวอย่าง)
* **Password:** `123`

---

## 🛠️ การติดตั้ง (Installation)

หากเครื่องใครยังไม่เคยลง Robot Framework ให้เปิด Terminal แล้วรันคำสั่งตามนี้:

```bash
# 1. ลง Python (ถ้ายังไม่มี)
# 2. ลง Library ที่ต้องใช้
pip install robotframework
pip install robotframework-seleniumlibrary
```
🚀 วิธีการรัน (How to Run)
Start Server: ตรวจสอบว่าเปิด Server ของตัวเว็บ (Backend/Frontend) ไว้เรียบร้อยแล้ว
Open Terminal: เข้ามาที่ Path ของโฟลเดอร์นี้

Execute: พิมพ์คำสั่งด้านล่าง
# เปลี่ยนชื่อไฟล์เป็นชื่อไฟล์จริงๆ ของเรา
robot test_create_project.robot

---
📂 โครงสร้างไฟล์ (File Structure)
.
├── test_create_project.robot   # 📄 ไฟล์เทสหลัก (Main Script)
├── resource.robot              # 🧩 (Optional) เก็บ Keyword กลาง/ตัวแปร
├── log.html                    # 📊 Log อย่างละเอียด (ดูไฟล์นี้เมื่อ Error)
└── report.html                 # 📈 Report สรุปผลการเทส (Pass/Fail)

📖 คู่มือการอ่าน Code สำหรับมือใหม่ (Beginner's Guide)
สำหรับเพื่อนที่เพิ่งเคยเห็น Robot Framework ครั้งแรก ไม่ต้องตกใจ! โค้ดมันอ่านง่ายเหมือนภาษาอังกฤษเลย แต่อยากให้อ่านตรงนี้ก่อนแก้โค้ดนะ

1. กฎเหล็ก: "เว้นวรรคผิด ชีวิตเปลี่ยน" ⚠️
Robot Framework "ซีเรียสเรื่องช่องว่าง (Space) มาก"

1 เคาะ = ถือว่าเป็นประโยคเดียวกัน (Text)

2 เคาะขึ้นไป (หรือ Tab) = ถือว่าเป็นการแบ่งคำสั่ง (Separator)

ตัวอย่าง:
    Input Text    id=username    admin      <-- ถูกต้อง (เว้นช่วงกว้างๆ)
    Input Text id=username admin            <-- ผิด! บอทจะงงว่าทำไมคำสั่งยาวจัง
Tip: แนะนำให้ลง Extension ใน VS Code ชื่อ "Robot Framework Language Server" มันจะช่วยจัดสีและเว้นวรรคให้เอง

2. คำศัพท์คำสั่งที่เจอบ่อยในไฟล์นี้ vocab
ในไฟล์ project.robot และ resource เราใช้คำสั่งพวกนี้บ่อยมาก:
### 🔑 คำสั่งที่เจอบ่อย (Common Keywords)

| คำสั่ง (Keyword) | คำแปลง่ายๆ | ตัวอย่างการใช้ |
| :--- | :--- | :--- |
| `Open Browser` | เปิด Chrome ขึ้นมา | `Open Browser  www.google.com  chrome` |
| `Input Text` | พิมพ์ข้อความลงช่อง | `Input Text  id=email  myname@test.com` |
| `Click Element` | คลิกปุ่มหรือลิ้งก์ | `Click Element  xpath=//button` |
| `Wait Until Element Is Visible` | **สำคัญมาก!** รอให้ของโผล่มาก่อนค่อยทำต่อ (กันบอทใจร้อน) | `Wait Until Element Is Visible  id=submit_btn  10s` |
| `Sleep` | สั่งให้หยุดนิ่งๆ สักพัก (หน่วยเป็นวินาที) | `Sleep  5s` |
| `Handle Alert` | จัดการกับ Popup แจ้งเตือนของ Browser (กด OK) | `Handle Alert  action=ACCEPT` |
| `Should Contain` | เช็คว่าข้อความนี้ "มีอยู่จริงไหม" (ใช้ตรวจการบ้าน) | `Should Contain  ${alert_msg}  บันทึกสำเร็จ` |

3. เรื่องของ "Locator" (บอทรู้ได้ไงว่าต้องกดตรงไหน?) 📍
เวลาเราสั่ง Click Element เราต้องบอก "ที่อยู่" ของปุ่มนั้นให้บอทรู้ เราเรียกว่า Locator

ในไฟล์นี้จะเห็นพวก id=..., name=..., หรือ xpath=... นั่นแหละคือที่อยู่ของปุ่ม

🕵️‍♂️ วิธีหา Locator ด้วยตัวเอง (เผื่อต้องแก้)
    เปิดหน้าเว็บที่ต้องการเทสใน Chrome
    เอาเมาส์ไปชี้ที่ปุ่มหรือช่องที่ต้องการ
    คลิกขวา เลือก Inspect (ตรวจสอบ)
    แถบ Code ด้านขวาจะเด้งขึ้นมา และไฮไลท์บรรทัดของปุ่มนั้น

วิธีดู:

ถ้ามี id="submit" → โชคดีมาก! ใช้ id=submit ได้เลย
ถ้ามี name="user" → ใช้ name=user ได้เลย
ถ้าไม่มีอะไรเลย? → คลิกขวาที่บรรทัด Code นั้น > Copy > Copy XPath แล้วเอามาใส่แบบ xpath=//...

4. โครงสร้างไฟล์
Variables: คือตัวแปร เอาไว้เก็บค่าที่ใช้บ่อยๆ เช่น URL หรือ PASSWORD เวลาแก้จะได้แก้ที่เดียว
Keywords: เหมือน Function คือการรวมคำสั่งย่อยๆ เป็นก้อนเดียวกัน เช่น Login to SE project ข้างในก็จะประกอบด้วย (กรอกชื่อ -> กรอกรหัส -> กดปุ่ม)
Test Cases: คือสถานการณ์ที่เราจะเทสจริง (Test Scenario) เช่น "กรอกข้อมูลครบต้องผ่าน", "ไม่กรอกรหัสต้องแจ้งเตือน"